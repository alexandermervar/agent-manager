services:
  council:
    build: .
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./agents:/app/agents
      - council-db:/app/data
    environment:
      - AGENTMGR_DB=/app/data/agent_manager.db

volumes:
  council-db:
